const {levelOrderTraversal} = require('./BreathFirstTraversal/levelOrderTraversal');

/* BINARY TREE - not a binary search tree!!
 ---------------------------------------
 we always try to shrink tree.
 we replace deleted value with the rightmost bottom value
 how:
     find desired value to delete
     find bottom most right value
     swap these to values
     delete bottom most right value
 */

function deleteNodeInABinaryTree(tree, valueToDelete) {
    const nodeToDelete = null;
    const bottomRightMostNode = null;

    // level order traversal

}